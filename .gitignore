.DS_Store
node_modules/
dist/

# React Router
/.react-router/
/build/

# secrets
.env
/files/
yarn.lock
respaldo.json
.mcp.json
.claude/
pdf_examples/
scripts/_backups/

# Runtime logs (Winston) — no deben trackearse
*.log

# Llave privada Ed25519 del MCP Registry (namespace cloud.easybits/*). NUNCA al repo.
*.pem
mcp-registry-key.pem
