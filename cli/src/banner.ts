// Banner de la portada del CLI (braille + color ANSI 24-bit, máx. 80 columnas).
// Sólo se imprime en `easybits` / `easybits --help`, con stdout en TTY, sin NO_COLOR y sin --json.
// PENDIENTE: la constante pre-generada no llegó; mientras, cadena vacía → no se imprime nada.
export const BANNER: string = ``;
