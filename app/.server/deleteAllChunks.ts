import { listObjectsInFolder } from "react-hook-multipart";

export const deleteAllChunkFor = async (storageKey: string) => {
  const storageKeysToDelete = [];
  // @todo request deletion to hono converter
};
